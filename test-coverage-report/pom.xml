<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.vanillabp</groupId>
    <artifactId>hazelcast-shared-election-cache-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>hazelcast-shared-election-cache-test-coverage-report</artifactId>
  <packaging>pom</packaging>
  <name>VanillaBP shared election cache on Hazelcast - Test coverage report</name>
  <description>Aggregator of the per-platform test coverage reports. Coverage is measured separately for each platform since a platform's tests never cover the other platform's code.</description>

  <modules>
    <module>quarkus</module>
    <module>spring-boot</module>
    <!-- last: it judges the reports the two modules above produced -->
    <module>coverage-gate</module>
  </modules>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-deploy-plugin</artifactId>
        <version>3.1.4</version>
        <configuration>
          <!-- nothing to deploy - reports are published on Github pages -->
          <skip>true</skip>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
