name: vanillabp-hazelcast-election-cache
artifact: "${project.groupId}:${project.artifactId}:${project.version}"
dependencies:
  - vanillabp
capabilities:
  provides:
    - io.vanillabp.election-cache.hazelcast
